spring:
  datasource:
    url: jdbc:h2:mem:purrrfolio;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true

purrrfolio:
  public-base-url: http://localhost:8080
  telegram:
    bot-token: replace-me
    webhook-secret: replace-me
    bot-username: purrrfolio_bot
    polling-enabled: true
