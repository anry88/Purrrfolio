spring:
  datasource:
    url: jdbc:h2:mem:purrrfolio;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true

purrrfolio:
  telegram:
    bot-token: test-token
    webhook-secret: test-secret
